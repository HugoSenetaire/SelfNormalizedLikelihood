defaults:
  - base_proposal_config_name
proposal_name : 'cnf'
cnf_divergence_fn: "approximate"  # Used in CNF proposal CHoice approximate, exact
cnf_method: "euler"  # Used in CNF proposal Choice euler, rk4, dopri5
cnf_nb_solver_step: 100 # Used in CNF proposal
cnf_T: 1 # Used in CNF proposal
