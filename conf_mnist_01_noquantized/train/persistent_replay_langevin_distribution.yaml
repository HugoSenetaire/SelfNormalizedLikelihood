defaults:
  - base_train_config_name
trainer_name: 'persistent_replay_langevin'
max_steps: null
max_epochs: 1000
output_folder: './Results'
load_from_checkpoint: false
seed: 42
just_test: false
decay_ema: null
task: 'distribution_estimation'
save_dir: null
multi_gpu: single
val_check_interval: null
save_energy_every: 100
samples_every: 1000
log_every_n_steps: 10
sigma_data: 1e-2

